<% layout('layouts/boilerplate') %>
<div class="col-6 offset-3">
    <form action="/client/select/<%=fileId%>/" class="validated-form" method="POST">
        <div class="mb-3">
            <h2 class="text-center">New Entry</h2>
            <% for(let header of headers) { %> 
            <label class="form-label" for="<%= header %>"><%= header %> :</label>
            <input class="form-control" type="text" name="<%= header %>" id="<%= header %>">
            <% } %> 
        </div>
        <button class="btn btn-primary">Add Entry</button>
    </form>
</div>